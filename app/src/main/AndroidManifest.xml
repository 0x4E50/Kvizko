<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.school.denis_niko.kvizko"
    android:versionName="1.0.0">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MainActivity -->
        <activity
            android:name="com.school.denis_niko.kvizko.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- TimerActivity -->
        <activity
            android:name="com.school.denis_niko.kvizko.TimerActivity"
            android:label="Timer"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait" />

        <!-- QuizActivity -->
        <activity android:name="com.school.denis_niko.kvizko.QuizActivity"
            android:label="Quiz"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- StatsActivity -->
        <activity
            android:name="com.school.denis_niko.kvizko.StatsActivity"
            android:label="Statistics"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>
